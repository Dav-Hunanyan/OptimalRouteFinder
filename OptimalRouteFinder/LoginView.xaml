<Window x:Class="OptimalRouteFinder.LoginView"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:OptimalRouteFinder.ViewModels"
        Height="420"
        Width="300"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#F3F6FB">

    <Border CornerRadius="12"
            Padding="25"
            Background="White"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Effect="{DynamicResource DropShadow}">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Optimal Route Finder"
                           FontSize="22"
                           FontWeight="SemiBold"
                           Foreground="#2C3E50"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="Sign in to continue"
                           FontSize="13"
                           Foreground="#7F8C8D"
                           HorizontalAlignment="Center"
                           Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Login Form -->
            <StackPanel Grid.Row="1">

                <TextBlock Text="Username"
                           Foreground="#34495E"
                           Margin="0,0,0,5"/>

                <TextBox Height="38"
                         FontSize="14"
                         Padding="10"
                         Background="#ECF0F1"
                         BorderThickness="0"
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,0,15"/>

                <TextBlock Text="Password"
                           Foreground="#34495E"
                           Margin="0,0,0,5"/>

                <PasswordBox Height="38" FontSize="14" Padding="10"
             Background="#ECF0F1" BorderThickness="0" Margin="0,0,0,25"
             helpers:PasswordBoxHelper.BindPassword="True"
             helpers:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay}" />

                <Button Content="Login"
                        Height="42"
                        FontSize="15"
                        FontWeight="SemiBold"
                        Background="#4A90E2"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Command="{Binding LoginCommand}">

                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <TextBlock Text="Don't have an account?"
                           HorizontalAlignment="Center"
                           Margin="0,20,0,0"
                           Foreground="#7F8C8D"/>

                <Button Content="Create Account"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="#4A90E2"
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        Command="{Binding GoToRegisterCommand}"
                        HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Border>
    <Window.Resources>
        <DropShadowEffect x:Key="DropShadow"
                          BlurRadius="20"
                          ShadowDepth="0"
                          Opacity="0.15"/>
    </Window.Resources>

</Window>